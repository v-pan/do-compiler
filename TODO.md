need to fix the span system, have it skip over connectives and whitespace
need to properly count span sizes for the parser to work right. maybe track end and size separately so spans can always be connected, but not cover the entire space necessarily
